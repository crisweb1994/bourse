// plan-v2 Wave 3.2 — 6 investor personas + persona registry removed
// alongside the DEBATE workflow. judgeNeutral remains because the
// COMPREHENSIVE workflow uses it through primitives/judge.ts for
// selective per-section auditing.
export type { Persona } from './types';
export { judgeNeutral } from './judge-neutral';
