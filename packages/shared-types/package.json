{
  "name": "@bourse/shared-types",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "lint": "echo 'no lint configured'"
  },
  "devDependencies": {
    "@bourse/tsconfig": "workspace:*",
    "typescript": "^5.7.0"
  }
}
